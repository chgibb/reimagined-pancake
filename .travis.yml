language: node_js
sudo: required
node_js:
    - "6"
install:
    - sudo apt-get update
    - sudo apt-get install g++
    - bash install.bash
script:
    - sh build.sh
notifications:
    email: never
